fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.7.4

torch            # CUDA 환경에 맞춰 설치 (도커 빌드 시 버전 지정 권장)
opencv-python-headless
numpy

chromadb==0.5.3
python-dotenv    # .env 로드 (db/chroma_db.py, server.py 등)
PyMySQL          # MySQL 사용 및 모듈 임포트 시 필요 (db/mysql.py)
# 권장: Chroma의 HNSW 인덱싱 백엔드. 보통 chromadb가 함께 설치하지만 누락 시 명시 설치
hnswlib>=0.7.0
Pillow           # structures.DetectionResult에서 이미지/폰트 렌더링에 사용
